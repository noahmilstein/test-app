import { render } from '@testing-library/react'
import { BasicTabs } from '../'

describe('BasicTabs', () => {
  it('WORKING HERE :: TEST ME', () => {
    render(<BasicTabs />)
    expect(1).toEqual(1)
  })
})
